name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env:
      VULTR_API: ${{secrets.VULTR_API}}
      STARTER_USER_EMAIL: ${{secrets.STARTER_USER_EMAIL}}
      STARTER_USER_PASS: ${{secrets.STARTER_USER_PASS}}
      STARTER_USER_BASE: ${{secrets.STARTER_USER_BASE}}
      STARTER_USER_TEAM: ${{secrets.STARTER_USER_TEAM}}

    # steps:
    # - uses: actions/checkout@v4
    # - uses: actions/setup-node@v4
    #   with:
    #     node-version: lts/*
    # - name: Install dependencies
    #   run: npm ci
    # - name: Install Playwright Browsers
    #   run: npx playwright install --with-deps
    # - name: Run Playwright tests
    #   run: npx playwright test tests/blog-page/blog_page_break_test.spec.ts --project=blog --workers=5
    # - uses: actions/upload-artifact@v4
    #   if: ${{ !cancelled() }}
    #   with:
    #     name: playwright-report
    #     path: playwright-report/
    #     retention-days: 30